import { Wrapper } from 'components/Wrapper';
import Base from '../templates/Base';

const Chat = () => {
  return (
    <Base>
      <Wrapper>
        <h1>Chat</h1>
      </Wrapper>
    </Base>
  );
};

export default Chat;
