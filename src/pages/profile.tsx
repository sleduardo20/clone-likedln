import Base from '../templates/Base';

import PageProfile from '../templates/Profile';

const Profile = () => {
  return (
    <Base>
      <PageProfile />
    </Base>
  );
};

export default Profile;
